BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[NostrEvent] ALTER COLUMN [value] NVARCHAR(max) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
